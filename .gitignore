node_modules/
downloads/*
!downloads/.gitkeep
# Local keeps: audio you want to survive, never committed (large binaries).
library/
.DS_Store
*.log
.env
# Local power-mode: machine-specific env + the Demucs venv. Never committed;
# the hosted deploy neither has nor needs any of this.
.env.local
.venv-demucs/
cookies*.txt
